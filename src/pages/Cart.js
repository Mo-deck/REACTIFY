import CartProducts from "../components/CartProducts";

const Cart = ()=>{
    
    return (
        <div className="cart-container">
            <CartProducts />
        </div>
    );
}

export default Cart;